#
#   function c.l.c:slot_checks/min_count
#   @parent c.l.c:slot_checks/slot_counts
#   @Memo Finding the Slot with the Least amount of Items
#
## Reset Scoreboards 
scoreboard players set Slot_Min clc_main 0

## Offset for empty slots 
function c.l.c:slot_checks/offset_slots

##                              Compare Item.[Slot].Count

## Slot 1
execute if score #current_count.1 clc_slot_count <= #current_count.2 clc_slot_count if score #current_count.1 clc_slot_count <= #current_count.3 clc_slot_count if score #current_count.1 clc_slot_count <= #current_count.10 clc_slot_count if score #current_count.1 clc_slot_count <= #current_count.11 clc_slot_count if score #current_count.1 clc_slot_count <= #current_count.12 clc_slot_count if score #current_count.1 clc_slot_count <= #current_count.19 clc_slot_count if score #current_count.1 clc_slot_count <= #current_count.20 clc_slot_count if score #current_count.1 clc_slot_count <= #current_count.21 clc_slot_count store result score Slot_Min clc_main run scoreboard players get #current_count.1 clc_slot_count
## Slot 2
execute if score #current_count.2 clc_slot_count <= #current_count.1 clc_slot_count if score #current_count.2 clc_slot_count <= #current_count.3 clc_slot_count if score #current_count.2 clc_slot_count <= #current_count.10 clc_slot_count if score #current_count.2 clc_slot_count <= #current_count.11 clc_slot_count if score #current_count.2 clc_slot_count <= #current_count.12 clc_slot_count if score #current_count.2 clc_slot_count <= #current_count.19 clc_slot_count if score #current_count.2 clc_slot_count <= #current_count.20 clc_slot_count if score #current_count.2 clc_slot_count <= #current_count.21 clc_slot_count store result score Slot_Min clc_main run scoreboard players get #current_count.2 clc_slot_count
## Slot 3
execute if score #current_count.3 clc_slot_count <= #current_count.1 clc_slot_count if score #current_count.3 clc_slot_count <= #current_count.2 clc_slot_count if score #current_count.3 clc_slot_count <= #current_count.10 clc_slot_count if score #current_count.3 clc_slot_count <= #current_count.11 clc_slot_count if score #current_count.3 clc_slot_count <= #current_count.12 clc_slot_count if score #current_count.3 clc_slot_count <= #current_count.19 clc_slot_count if score #current_count.3 clc_slot_count <= #current_count.20 clc_slot_count if score #current_count.3 clc_slot_count <= #current_count.21 clc_slot_count store result score Slot_Min clc_main run scoreboard players get #current_count.3 clc_slot_count
## Slot 10
execute if score #current_count.10 clc_slot_count <= #current_count.1 clc_slot_count if score #current_count.10 clc_slot_count <= #current_count.2 clc_slot_count if score #current_count.10 clc_slot_count <= #current_count.3 clc_slot_count if score #current_count.10 clc_slot_count <= #current_count.11 clc_slot_count if score #current_count.10 clc_slot_count <= #current_count.12 clc_slot_count if score #current_count.10 clc_slot_count <= #current_count.19 clc_slot_count if score #current_count.10 clc_slot_count <= #current_count.20 clc_slot_count if score #current_count.10 clc_slot_count <= #current_count.21 clc_slot_count store result score Slot_Min clc_main run scoreboard players get #current_count.10 clc_slot_count
## Slot 11
execute if score #current_count.11 clc_slot_count <= #current_count.1 clc_slot_count if score #current_count.11 clc_slot_count <= #current_count.2 clc_slot_count if score #current_count.11 clc_slot_count <= #current_count.3 clc_slot_count if score #current_count.11 clc_slot_count <= #current_count.10 clc_slot_count if score #current_count.11 clc_slot_count <= #current_count.12 clc_slot_count if score #current_count.11 clc_slot_count <= #current_count.19 clc_slot_count if score #current_count.11 clc_slot_count <= #current_count.20 clc_slot_count if score #current_count.11 clc_slot_count <= #current_count.21 clc_slot_count store result score Slot_Min clc_main run scoreboard players get #current_count.11 clc_slot_count
## Slot 12
execute if score #current_count.12 clc_slot_count <= #current_count.1 clc_slot_count if score #current_count.12 clc_slot_count <= #current_count.2 clc_slot_count if score #current_count.12 clc_slot_count <= #current_count.3 clc_slot_count if score #current_count.12 clc_slot_count <= #current_count.10 clc_slot_count if score #current_count.12 clc_slot_count <= #current_count.11 clc_slot_count if score #current_count.12 clc_slot_count <= #current_count.19 clc_slot_count if score #current_count.12 clc_slot_count <= #current_count.20 clc_slot_count if score #current_count.12 clc_slot_count <= #current_count.21 clc_slot_count store result score Slot_Min clc_main run scoreboard players get #current_count.12 clc_slot_count
## Slot 19
execute if score #current_count.19 clc_slot_count <= #current_count.1 clc_slot_count if score #current_count.19 clc_slot_count <= #current_count.2 clc_slot_count if score #current_count.19 clc_slot_count <= #current_count.3 clc_slot_count if score #current_count.19 clc_slot_count <= #current_count.10 clc_slot_count if score #current_count.19 clc_slot_count <= #current_count.11 clc_slot_count if score #current_count.19 clc_slot_count <= #current_count.12 clc_slot_count if score #current_count.19 clc_slot_count <= #current_count.20 clc_slot_count if score #current_count.19 clc_slot_count <= #current_count.21 clc_slot_count store result score Slot_Min clc_main run scoreboard players get #current_count.19 clc_slot_count 
## Slot 20
execute if score #current_count.20 clc_slot_count <= #current_count.1 clc_slot_count if score #current_count.20 clc_slot_count <= #current_count.2 clc_slot_count if score #current_count.20 clc_slot_count <= #current_count.3 clc_slot_count if score #current_count.20 clc_slot_count <= #current_count.10 clc_slot_count if score #current_count.20 clc_slot_count <= #current_count.11 clc_slot_count if score #current_count.20 clc_slot_count <= #current_count.12 clc_slot_count if score #current_count.20 clc_slot_count <= #current_count.19 clc_slot_count if score #current_count.20 clc_slot_count <= #current_count.21 clc_slot_count store result score Slot_Min clc_main run scoreboard players get #current_count.20 clc_slot_count 
## Slot 21
execute if score #current_count.21 clc_slot_count <= #current_count.1 clc_slot_count if score #current_count.21 clc_slot_count <= #current_count.2 clc_slot_count if score #current_count.21 clc_slot_count <= #current_count.3 clc_slot_count if score #current_count.21 clc_slot_count <= #current_count.10 clc_slot_count if score #current_count.21 clc_slot_count <= #current_count.11 clc_slot_count if score #current_count.21 clc_slot_count <= #current_count.12 clc_slot_count if score #current_count.21 clc_slot_count <= #current_count.19 clc_slot_count if score #current_count.21 clc_slot_count <= #current_count.20 clc_slot_count store result score Slot_Min clc_main run scoreboard players get #current_count.21 clc_slot_count